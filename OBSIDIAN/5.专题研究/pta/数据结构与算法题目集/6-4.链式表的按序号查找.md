```c
ElementType FindKth( List L, int K ){
    int length = 0;
    List p = L;
    while(p){
        length++;
        if(length == K) return p->Data;
        p = p->Next;
    }
    return ERROR;
}
```